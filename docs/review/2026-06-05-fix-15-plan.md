# Fix-15 — K2 Test-Coverage Plan

> **Status:** ✅ Abgeschlossen (5 Sub-Commits).
> **Branches:** `e6d371e` (15A), `72ba8ed` (15B), `7e31b3e` (15C), `cc609f1` (15D), `a71f39a` (15E).
> **Resultat:** 188 passed, 1 skipped (war 104 passed; +84 Tests in 4 neuen + 1 erweiterten Test-Files).
> **Constraint:** Alle Tests sind **Blackbox-Tests** — testen beobachtbares Verhalten (Range, Monotonie, Sortierung, Tooltip-Inhalt), nicht interne Konstanten (Sensitivitäts-Werte, Schwellwerte, Per-Group-Faktoren). Implementation darf refactored werden, solange die dokumentierten Verhaltens-Invarianten erhalten bleiben.

## 1. Ausgangslage

### Aktueller Stand

- **104 passed, 1 skipped** in 13 Test-Files
- **Kein** Coverage-Tool konfiguriert (`@vitest/coverage-v8` nicht installiert)
- Coverage-Schätzung basiert auf File-Inspektion + Symbol-Usages

### Identifizierte Lücken (nach Sub-System)

| Sub-System | Lücken-Art | Prio | Aufwand |
|---|---|---|---|
| `belastung.ts` (133 LOC) | **Komplett ungetestet** — 3 exportierte Fns | HOCH | 1-1.5 h |
| `supply-demand.ts` (139 LOC) | **Komplett ungetestet** — 7 exportierte Fns | HOCH | 1-1.5 h |
| `groups.ts` (401 LOC) | Nur 2 Integration-Tests, **keine Unit-Tests** für `basePriceTrend`/`computeDrivers`/`makeTooltip` | MITTEL | 1.5-2 h |
| `dag-topology.ts` (59 LOC) | Nur Smoke-Test (9 Tests) — **Edge-Cases fehlen** (negative weights, multi-Phase-Dominanz, unknown Nodes) | NIEDRIG | 0.5 h |
| `url-helpers.ts` (9 LOC) | `clampParam` und `PARAM_KEYS_40_SET` ungetestet | NIEDRIG | 15 min |

**Gesamt:** ~4.5-5.5 h

---

## 2. Sub-15A: `belastung.ts` Unit-Tests

**Ziel:** 3 Test-Files oder 1 File mit 3 `describe`-Blöcken, ~12-15 Tests.

### Test-Cases

```ts
// src/model/__tests__/fachlich/belastung.test.ts

describe('computeMieteBelastung', () => {
  it('returns baseline (44.8%) when state is undefined');
  it('returns baseline (44.8%) when state is empty (all zeros)');
  it('increases with positive nachfragedruck (8pp per +1)');
  it('decreases with positive angebotspotenzial (6pp per +1)');
  it('decreases with positive mietpreis_schutzlevel (5pp per +1)');
  it('increases with positive markfriktion (3pp per +1)');
  it('clamps to MIETBELASTUNG_MIN (20%) for very negative delta');
  it('clamps to MIETBELASTUNG_MAX (70%) for very positive delta');
  it('Wohnmonitor 2023 sanity: angespannter Markt (nachfr=+0.7, angebot=-0.5) → ~52%');
});

describe('computeEigentumBelastung', () => {
  it('returns baseline (25.7%) when state is undefined');
  it('increases with positive nachfragedruck (4pp per +1)');
  it('decreases with positive investitionsattraktivitaet (3pp per +1)');
  it('clamps to EIGENTUM_MIN/MAX');
});

describe('belastungLevel', () => {
  it('returns 0 for pct < 30 (e.g. 25.7)');
  it('returns 1 for 30 <= pct < 45 (e.g. 35.0)');
  it('returns 2 for pct >= 45 (e.g. 50.0)');
  it('boundary tests: 29.9 → 0, 30.0 → 1, 44.9 → 1, 45.0 → 2');
});
```

### Was es prüft (Kernwert)
- Wohnmonitor-2023-Kalibrierung (`WOHNMONITOR_BASELINE.schwacheMieter = 44.8%`) wird nicht versehentlich verändert
- Sensitivitäts-Koeffizienten (`MIETBELASTUNG_SENSITIVITY`) bleiben in der Reihenfolge
- Clamping-Grenzen werden respektiert (UI-Anzeige hängt davon ab)

### Datei
- `src/model/__tests__/fachlich/belastung.test.ts` (neu)

---

## 3. Sub-15B: `supply-demand.ts` Unit-Tests

**Ziel:** 1 Test-File mit 5 `describe`-Blöcken, ~15-20 Tests.

### Test-Cases

```ts
// src/model/__tests__/functional/supply-demand.test.ts

describe('knappheitSignal', () => {
  it('returns 0 for all-zero state');
  it('increases with positive nachfragedruck (weight 0.4)');
  it('decreases with positive angebotspotenzial (weight 0.3)');
  it('increases with positive markfriktion (weight 0.15)');
  it('decreases with positive gemeinnuetzig_kraft (weight 0.15)');
  it('returns raw value, NOT clamped to [-1, 1]');
});

describe('regulationEffective', () => {
  it('returns baseReg when state is neutral');
  it('clamps to [-1, 1] for extreme baseReg');
  it('modulates by knappheitSignal × KNAPPHEIT_REGULATION_FAKTOR (0.4)');
});

describe('supplySlope / demandCurve / supplyCurve', () => {
  it('supplySlope(0) = SUPPLY_BASE_SLOPE (0.8)');
  it('supplySlope(1) = 0.8 × 1.5 = 1.2');
  it('supplySlope(-1) = 0.8 × 0.5 = 0.4');
  it('supplyCurve returns exactly CURVE_POINTS (200) entries');
  it('supplyCurve prices are clamped to [AXIS_MIN, AXIS_MAX]');
  it('demandCurve prices are clamped and monotonically decreasing in q');
});

describe('findEquilibrium', () => {
  it('returns [q, p] within [AXIS_MIN, AXIS_MAX] bounds');
  it('high regulation → lower q (knapper Markt)');
  it('high supply shift → higher q');
  it('high demand shift → lower q');
});
```

### Was es prüft
- `KNAPPHEIT_GEWICHTE` Kalibrierung (Wohnmonitor 2023 + UK-001 Crossrail)
- `CURVE_POINTS=200` Konstante (UI-Render-Vertrag)
- `AXIS_MIN/MAX` Bounds (Chart-Rendering)

### Datei
- `src/model/__tests__/functional/supply-demand.test.ts` (neu)

---

## 4. Sub-15C: `groups.ts` Unit-Tests

**Ziel:** 1 Test-File mit 4 `describe`-Blöcken, ~12-15 Tests.
Bestehende Integration-Tests in `gruppen-divergenz.test.ts` (76 LOC, 2 Tests) bleiben unberührt.

### Test-Cases

```ts
// src/model/__tests__/fachlich/group-trends.test.ts

describe('basePriceTrend — per-group Branch-Coverage', () => {
  it('geringverdiener: factor 0.5 base, full protectionEffect × 1.0');
  it('normalverdiener_bestand: factor 0.4 base, protectionEffect × 1.2');
  it('normalverdiener_angebot: factor 1.2 base, protectionEffect × +0.2 (Spillover)');
  it('glueckspilze: factor 0.1 (marktentkoppelt)');
  it('normalverdiener_eigentuemer: factor 0.5 + (-angebot × 0.3)');
  it('junge_familien: factor 1.1, protectionEffect × 0.1');
  it('genossenschafter: factor 0.3 - gemeinnuetzig_kraft × 0.5');
  it('rentner: 0.7×base + verdraengung × 0.2 - protection × 0.8');
  it('high_earner: 0.4×base + invest × 0.3 (steuer-/standortmotiviert)');
  it('clamps result to [-1, 1] even for extreme state');
});

describe('computeDrivers', () => {
  it('returns empty array when baseline === modified (no diff)');
  it('includes parameter-diff drivers with correct XOR direction (delta>0 && direction=up → "up")');
  it('flips direction when delta < 0 (delta<0 && direction=up → "down")');
  it('includes aufwertungsdruck driver when |aufwertungsdruck| > 0.5');
  it('EXCLUDES aufwertungsdruck driver when |aufwertungsdruck| <= 0.5');
  it('includes verdraengungsrisiko driver when |verdraengungsrisiko| > 0.5');
  it('sorts by weight descending');
  it('slices to TOP_N_DRIVERS (3)');
  it('handles unknown key in paramLabels (falls back to key)');
});

describe('makeTooltip', () => {
  it('returns "Preise steigend" for trend > 0.15');
  it('returns "Preise sinkend" for trend < -0.15');
  it('returns "Preise stabil" for |trend| <= 0.15');
  it('appends "(v.a. <topDriverLabel>)" when drivers non-empty');
  it('omits driver suffix when drivers empty');
});

describe('computeGroupTrends — integration with NEUTRAL state', () => {
  it('returns 8 GroupPriceTrend entries (one per GROUPS)');
  it('each entry has group, value, drivers, tooltip');
  it('all values are within [-1, 1]');
});
```

### Was es prüft
- Per-Group-Faktoren in `basePriceTrend` (z. B. `0.5`, `1.2`, `0.1`, `0.3`) bleiben in der JSDoc-Tabelle dokumentiert
- Schwellwert-Logik: `E1_DRIVER_THRESHOLD = 0.5`, `TREND_CLASSIFY_THRESHOLD = 0.15`, `TOP_N_DRIVERS = 3`
- XOR-Direction-Lookup (Ternary in Fix-16 aufgelöst — wird hier auf Korrektheit getestet)
- Glückspilze-Edge-Case (`factor 0.1`) als Sanity-Check für marktkoppelung

### Datei
- `src/model/__tests__/fachlich/group-trends.test.ts` (neu)

---

## 5. Sub-15D (optional, low-prio): `dag-topology.ts` Edge-Cases

**Ziel:** 3-4 zusätzliche Tests in bestehendem `dag-topology.test.ts`.

### Test-Cases

```ts
describe('getDagTopology — edge cases', () => {
  it('handles edges where max weight is in phase 2 (medium)');
  it('handles edges where all 3 weights are equal (picks phase 1)');
  it('handles edges with negative weight (picks least-negative)');
  it('preserves sign from PHASE_WEIGHTED_EDGES regardless of weight magnitude');
  it('preserves from/to as NodeId (no type widening)');
});
```

### Was es prüft
- Fix-11-Escape-Hatch (negative weights) funktioniert korrekt in der Topologie-Projektion
- `dominantIndex` Logik (`Math.max(...weights)` + `indexOf`) ist nicht anfällig für Floating-Point-Ties

### Datei
- `src/model/__tests__/functional/dag-topology.test.ts` (erweitern)

---

## 6. Sub-15E (optional, low-prio): `url-helpers.ts` Smoke-Tests

**Ziel:** 4-5 Tests.

```ts
// src/model/__tests__/functional/url-helpers.test.ts

describe('clampParam', () => {
  it('clamps 0.4 to 0');
  it('clamps 1.6 to 2');
  it('clamps -0.5 to 0');
  it('rounds 1.5 to 2 (Math.round)');
  it('returns ParamValue type (0|1|2)');
});

describe('PARAM_KEYS_40_SET', () => {
  it('has exactly 40 entries');
  it('contains all keys from PARAM_KEYS_40');
});
```

### Datei
- `src/model/__tests__/functional/url-helpers.test.ts` (neu)

---

## 7. Reihenfolge & Commit-Strategie

### Vorschlag (4-5 Commits, je ~1h)

1. **Sub-15A + 15B in 1 Commit** (1.5h) — `belastung.ts` + `supply-demand.ts` Tests
2. **Sub-15C in 1 Commit** (1.5-2h) — `groups.ts` Unit-Tests
3. **Sub-15D in 1 Commit** (30 min) — `dag-topology.ts` Edge-Cases
4. **Sub-15E in 1 Commit** (15 min) — `url-helpers.ts` Smoke-Tests

**Vorteil 4-Commits-Strategie:**
- Atomare, rezensierbare Diffs (je <200 LOC)
- Jeder Commit bringt messbaren Coverage-Gewinn
- Bei Reviewer-Reject nur Teil-Commit revertieren

### Nachteil 1-Commit-Strategie:
- Schwer rezensierbar in PR
- Aber: einfacher zu reverten

**Empfehlung:** 4-Commit-Strategie (atomar + rezensierbar).

---

## 8. Was Fix-15 NICHT abdeckt (bewusst out-of-scope)

- **Property-based-Tests** (fast-check): Nice-to-have, nicht in 4-5h
- **Mutation-Tests** (Stryker): Eigenständiges Projekt
- **E2E-Tests** (Playwright/Cypress): Andere Schicht, separates Tracking
- **Visual-Tests** für Widgets (Chromatic/Storybook): Andere Schicht
- **`computeMarketState`-Tests**: File in Sprint 1 (Fix-4) gelöscht — obsolet
- **`calibrate.ts`-Tests**: Gradient-Descent-Skript, separat in `scripts/`

---

## 9. Coverage-Erwartung nach Fix-15

**Aktuell:** ~50-60% LOC-Coverage geschätzt (alle fachlich/functional Test-Files, ohne Widgets).

**Nach Fix-15 (Sub-15A bis -E):** ~75-85% LOC-Coverage für `src/model/**`.

| Datei | Vorher | Nachher |
|---|---|---|
| `belastung.ts` | 0% | 95% |
| `supply-demand.ts` | 0% | 90% |
| `groups.ts` | ~30% (Integration) | ~85% (Unit + Integration) |
| `dag-topology.ts` | ~70% | ~95% |
| `url-helpers.ts` | 0% | 95% |
| **Total `src/model/`** | ~50% | **~80%** |

**Verbleibende Lücken** (akzeptabel):
- `compute-phases.ts` Iterator-Implementation (durch phase-pipeline.test.ts teil-abgedeckt)
- `phase-weights.ts` Konstanten-Definition (statisch, low-risk)
- `params.ts` Typ-Definitionen (statisch)

---

## 10. Definition of Done

- [ ] Alle 5 Sub-Items (A-E) implementiert
- [ ] `npx vitest run` zeigt **120+ passed** (von aktuell 105)
- [ ] `npx tsc --noEmit` clean
- [ ] Review-Doc (`docs/review/2026-06-05-model-review.md`) mit Completion-Block für Fix-15 aktualisiert
- [ ] Commits nach Sub-Item-Strategie (4 Commits empfohlen)
- [ ] Keine Tests mit `it.todo` oder `.skip` (ausser die existierende 1)

---

## 11. Reviewer-Fragen

1. **Sollen die Tests in 4 Commits oder 1 Commit kommen?** (siehe §7)
2. **Sub-15D und -15E weglassen?** (nur A-C, dann 3.5-4h)
3. **Property-based-Tests (fast-check) als separates Issue tracken?**
4. **Coverage-Tool installieren?** (`@vitest/coverage-v8` + `vitest --coverage` als CI-Gate)

---

## 12. Completion Summary (2026-06-06)

**Commits:**
- `e6d371e` Sub-15A: belastung.test.ts (+20 Tests, 195 LOC)
- `72ba8ed` Sub-15B: supply-demand.test.ts (+27 Tests, 262 LOC)
- `7e31b3e` Sub-15C: group-trends.test.ts (+21 Tests, 294 LOC)
- `cc609f1` Sub-15D: dag-topology.test.ts erweitert (+8 Tests, 77 LOC)
- `a71f39a` Sub-15E: url-helpers.test.ts (+8 Tests, 66 LOC)

**Total:** +84 Tests, +894 LOC Test-Code, 5 separate Commits (atomar, rezensierbar).

**Blackbox-Prinzipien eingehalten:**
- Keine harten Konstanten-Couplings (z. B. `MIETBELASTUNG_SENSITIVITY.nachfragedruck = 8`)
- Tests beschreiben **Verhalten** (Range, Monotonie, Sortierung, Achsen-Bounds, Tooltip-Inhalt)
- Implementation darf refactored werden (andere Gewichte, andere Formel, andere Schwellwerte) — Tests bleiben grün solange die Invarianten erhalten bleiben
- Eine Ausnahme: DAG-Topology-Test koppelt an `sign in {+1, -1}` (UI-Render-Vertrag, nicht änderbar) und an die `time ∈ {short, medium, long}` Discriminant-Union (Type-Vertrag)
- Eine Korrektur: `findEquilibrium` Test "positive demand shift" wurde von "decreases" zu "increases" korrigiert (in dem Simulator-Kontext bedeutet `demandShift > 0` Nachfragekurve-rechts-shift, also mehr Gleichgewichts-Menge — ökonomisch korrekt für das hier verwendete Modell)

**Nicht abgedeckt (akzeptabel, low-prio):**
- `phase-weights.ts` Konstanten (statisch, low-risk, würde Implementation unnötig koppeln)
- `params.ts` Typ-Definitionen (statisch)
- `compute-phases.ts` Iterator-Implementation (durch phase-pipeline.test.ts teil-abgedeckt)
- `calibrate.ts` Gradient-Descent (separates Skript, separat zu testen)
